<?php 

/*
$page_id = (int) $website->page->id_route;

global $pages_info;

if($page_id > 0)
{
	$pages_info = new PageSingle($page_id);
}
else if(isset($website->fields['page_id']))
{
	$page_id_field = (int) $website->fields['page_id'];
	if($page_id_field > 0)
		{
			$page_id_field = (int) $website->fields['page_id'];
			$page_info = new PageSingle($page_id_field);
		}
}

*/